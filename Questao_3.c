#include <stdio.h>
#include <locale.h>
#define max 10

int Anagrama(char palavra[]);

int main()
{
  char palavra[max];

  prinft("~~~~~~~~Anagrama~~~~~~~~~\n\n");
  printf("Digite uma palavra para saber quantos anagramas existem dentro dela: ");
  scanf("%s", &palavra);
  Anagrama(palavra);

  return 0;
}

int Anagrama(char palavra[])
{
  int i, j, k, contagem = 0;

  // EM ANDAMENTO...
  for (i = 0; i < '\0'; i++)
  {
    for ()
  }
}